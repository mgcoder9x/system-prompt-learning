# Ghi chú — <<LESSON_TITLE>>

## Tóm tắt ngắn
(1–3 câu cốt lõi, viết khi đã học.)

## Tóm tắt sâu
(Giải thích đầy đủ, có thể dạy lại.)

## Claims

```yaml
claims: []
```

> Claim đặt DƯỚI `## Claims` (INV-23), mỗi claim `{id, class: A|B|C|D, status: draft|confirmed, text, ...}`
> (xem `rules/claim_rules.md`). Claim `draft` cấm vào Knowledge Map (INV-26).
