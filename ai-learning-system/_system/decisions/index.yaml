# Roll-up máy-đọc của decision journal.
# Nguồn sự thật đầy đủ nằm ở các file .md cùng thư mục; đây chỉ là bảng quét nhanh cho AI.
# Khi thêm/sửa entry ở .md → cập nhật dòng tương ứng ở đây (id không tái sử dụng).

meta:
  purpose: "nhật ký quyết định AI, xuyên suốt, để kiểm chứng"
  location: "_system/decisions/"
  schema_ref: "README.md"
  last_updated: 2026-07-09
  note_latest_current: "PHIÊN 2026-07-08 (máy toann, py3.13.12): (a) ĐỒNG BỘ NHẬT KÝ cho tính năng blueprint — bổ sung các entry còn THIẾU so với index: DEV-007 (bump spec v2.7→v2.8 qua CR-0014, theo tiền lệ DEV-003/006 mỗi bump spec là 1 DEV riêng), TRD-007 (QĐ-1 Coverage_Map ở CurriculumPoint.area_refs vs Blueprint.covered_by), NOTE-038 (khối đầy đủ audit blueprint — trước chỉ có trong index, nay ghi vào notes.md). (b) PHÁT HIỆN MỚI (đọc code thật): NOTE-039 giới hạn workflow — curriculum teachable dựng trước KHÔNG áp được blueprint approved về sau (không có retrofit area_refs), blueprint-first là luồng duy nhất; KHÔNG phải bug (validator đúng R3/R5), KHÔNG vi phạm req (R5.4 giữ). TRD-008 quyết định: GHI NHẬN ràng buộc (A) thay vì xây lệnh retrofit (B) — B là mở-rộng-scope cần CR (R7.1), chờ owner. (c) NOTE-040 chuyển máy lần 4 + re-verify 505 passed + cảnh báo bản sao lồng system-prompt2/. (d) THEO DUYỆT OWNER (phương án B của TRD-008, owner nói 'Có'): hiện thực CR-0015/DEC-076 — chế độ cờ /curriculum --set-area-refs (retrofit area_refs cho point đã có) GIẢI ngõ cụt NOTE-039 (curriculum-first→áp-khung). SPEC-first (CR design→valid→approve) rồi RED-first 7 unit + 1 E2E. NOTE-039→resolved, TRD-008→đóng(B). (e) THEO YÊU CẦU OWNER 'cần 1 AI khác xác định': hardening HANDOFF_TEST (DEC-077) — thêm Phần A3 (blueprint + retrofit, chứng minh NOTE-039 giải) + R4c + /blueprint + drift-guard, để AI KHÁC xác minh ĐẦY ĐỦ v2.8 (không chỉ v2.7). Cross-AI test THẬT chờ owner chạy AI khác (tôi KHÔNG tự chấm thay). 513→514 passed, validate --scope full PASS, selfcheck NGUYÊN VẸN. (f) THEO YÊU CẦU OWNER 'cần cách CỰC MẠNH chống drift': DEC-078 thêm drift-guard cho CHÍNH nhật ký decisions/ (test_decision_journal_consistency.py — song ánh index.yaml↔.md 2 chiều + id duy nhất + đặt đúng file + type khớp). Điều tra: nhật ký là artifact máy-đọc DUY NHẤT trước đây không có guard, mà nó là xương sống chống-drift → canh người-gác = fix gốc. Teeth-probe DEC-999 xác nhận có răng. 514→519 passed. KHÔNG push (owner dặn giữ local)."
  note_latest: "DEC-074/075: TÍNH NĂNG MỚI mandatory-curriculum-framework (Topic_Blueprint — khung giáo trình BẮT BUỘC) HOÀN TẤT 100% (SPEC-first requirements→design→tasks + 8 wave RED-first; CR-0011..0014 approved+changelog; spec v2.7→v2.8 §3.6). Thêm: schema blueprint (Blueprint+MandatoryArea) + CurriculumPoint.area_refs (Coverage_Map) + Blueprint_Validator 7 mã E-BP-* (DUP-ID/ORDER/EMPTY-TITLE/REF-BROKEN/AREA-REF-BROKEN cấu-trúc+ref áp cả draft; AREA-UNCOVERED/POINT-OUTSIDE phủ teachable-gated) + lệnh /blueprint (build/--edit/--approve/--amend --confirm, id ổn định R1.2). QĐ-1 Coverage_Map ở phía curriculum (editable) không blueprint (khóa khi approved). DEC-075 ROOT-FIX (phát hiện khi truy vết luồng approve TRƯỚC khi viết lệnh): phủ là CỔNG của teachable (gate approved+curriculum.teachable), KHÔNG bất biến vault độc lập → tránh brick-vault (lớp DEC-073) + chicken-egg approve. Baseline 458→499 passed (+41). validate --scope full PASS; selfcheck NGUYÊN VẸN. An toàn dài hạn: git init ở thư mục CHA + baseline commit + commit mỗi wave xanh (thư mục trước KHÔNG phải git repo). DEC-073 + NOTE-036/037 (trước đó): FIX BUG TÍCH HỢP THẬT E-EXAM-REF-BROKEN transaction-overlay (do cross-AI handoff #2 Gemini phát hiện, E2E cũ DEC-068 bỏ sót vì chỉ validate STANDALONE). Bug: _build_overlay copy nội-dung-vault sang TEMP, exam/ sibling NGOÀI vault không có trong overlay → _check_exam_results resolve ref bài nộp + exam_root vào TEMP → false-positive → MỌI lệnh FULL-transaction SAU khi 1 topic có exam_results.md bị ABORT oan. Fix GỐC (DEC-073, KHÔNG hack như Gemini 'tx_overlay_'+__file__ phá portability): thread real_vault_root=self.root qua validate_full_semantic→core→_validate_topic→_check_exam_results, dựng lại real_topic + exam_root từ vault THẬT; standalone giữ nguyên. RED-first probe (trước=abort False sau=True) + test_tx_exam_overlay.py 4 test (teeth still-detects broken ref). 458 passed (454+4), validator full --at PASS, selfcheck NGUYÊN VẸN. NOTE-037: chuyển máy lần 3 về toann (venv rebuild scoop py313, `py` không có). NOTE-036: chấm handoff #2 Gemini ĐẠT R1–R7 + ghi công phát hiện bug. Curriculum-driven-learning vẫn HOÀN TẤT 100%; KHÔNG còn task feature mở. Trước đó DEC-072 (fix test-defect bootstrap spec_ground_truth ngoài-đơn-vị, +2). Trước đó NOTE-035 (handoff #1 Claude Opus 4.6 ĐẠT, đóng NOTE-031). Trước đó NOTE-034 (chuyển máy THẬT lần 2 toann→k.nguyen.manh.toan: .venv cũ trỏ Python máy cũ → dựng lại bằng py 3.11.9; re-verify TOÀN BỘ trên máy mới = 452 passed + validator full scope PASS + selfcheck NGUYÊN VẸN; nâng portability transcript→ran-command trên máy thứ hai, 3.13-cũ vs 3.11-mới đều 452. Chỉ .venv dựng lại [đúng thiết kế INV-16], dữ liệu+journal thuần-file mang đi nguyên vẹn). Trước đó DEC-071 (audit đối kháng lệnh curriculum: fix GỐC crash traceback — 3 lệnh insert/next_lesson/grade đọc RAW points/results rồi .get() → AttributeError trên file sửa-tay-hỏng [YAML hợp lệ, collection sai kiểu]; helper chung _load_model_validated validate qua pydantic TRƯỚC iterate → E-SCHEMA sạch [NOTE-018]; grade curriculum-tham-chiếu-phụ dùng guard mềm isinstance. Probe CLI xác nhận trước=Traceback sau=E-SCHEMA. RED-first 5; 452 passed, validator PASS). Hệ SẴN SÀNG cross-AI test. Trước đó DEC-070 (hardening HANDOFF) + DEC-069 (T5.3 insert)"
  verified_legend:
    read-source: "đã đọc trực tiếp mã nguồn/file trong phiên"
    ran-command: "đã chạy lệnh và thấy output thật"
    ran-test: "đã chạy test và thấy kết quả thật"
    transcript: "chỉ dựa trên nhật ký phiên trước (end.md), CHƯA tái kiểm"

entries:
  # (1) decisions.md — quyết định tự ra
  - {id: DEC-001, type: decision,  status: active,        verified: true,  method: read-source, title: "Tiêu chí cụ thể INV-17 (E-MIX-CODE)"}
  - {id: DEC-002, type: decision,  status: active,        verified: true,  method: read-source, title: "Tiêu chí cụ thể INV-18 (E-MIX-DATA)"}
  - {id: DEC-003, type: decision,  status: superseded,    verified: false, method: transcript,  title: "Hoãn enforce INV-17/18 tới khi có tiêu chí", superseded_by: DEC-001}
  - {id: DEC-004, type: decision,  status: active,        verified: true,  method: read-source, title: "enable_fuzzing=False cho FSRS (tất định)"}
  - {id: DEC-005, type: decision,  status: active,        verified: true,  method: read-source, title: "Drift-guard test docs↔code"}
  - {id: DEC-006, type: decision,  status: active,        verified: true,  method: read-source, title: "Refactor _build_parser + CLI_COMMANDS"}
  - {id: DEC-007, type: decision,  status: active,        verified: true,  method: read-source, title: "Định dạng & vị trí decision journal"}
  - {id: DEC-008, type: decision,  status: active,        verified: true,  method: ran-test,    title: "schemas/ mô tả ràng-buộc-test, drift-guard vào models.py (không nguồn thứ 2)"}
  - {id: DEC-009, type: decision,  status: active,        verified: true,  method: ran-test,    title: "prompts/ (3 file) — router drift-guard vào commands.md + 7 intent spec 11; prose bám spec"}
  - {id: DEC-010, type: decision,  status: active,        verified: true,  method: ran-test,    title: "change_requests/ scaffolding + CR-0001 hồi tố (khép vết v2.6); drift-guard states↔folders"}
  - {id: DEC-011, type: decision,  status: active,        verified: true,  method: ran-test,    title: "P11: chỉ dựng planner tất định; hoãn execution/bước migration tới khi có schema v2 thật"}
  - {id: DEC-012, type: decision,  status: active,        verified: true,  method: ran-test,    title: "memory_rules + anti_drift_rules (khép rules/); tách code-enforced vs process-enforced"}
  - {id: DEC-013, type: decision,  status: active,        verified: true,  method: ran-test,    title: "templates/ đuôi .template.md (tránh INV-18) + topic rỗng hash view thật; golden test FULL PASS"}
  - {id: DEC-014, type: decision,  status: active,        verified: true,  method: ran-test,    title: "backend /learn tạo topic mới tất định từ template; Q1–Q3 calibrate là tầng AI, không ở CLI"}
  - {id: DEC-015, type: decision,  status: active,        verified: true,  method: ran-test,    title: "engine due 8.5 (schedule.py) dùng chung + /schedule read-only; days>0 là mở rộng của AI"}
  - {id: DEC-016, type: decision,  status: superseded-part, verified: true,  method: ran-test,    title: "/status read-only + /resume (cửa vào 11B); /status tái dùng engine 8.5; cảnh báo phiên chưa /done", superseded_part_by: DEV-005}
  - {id: DEC-017, type: decision,  status: active,        verified: true,  method: ran-test,    title: "E2E composition test (learn→status→review→done→forget, FULL PASS mỗi bước) — nghiệm thu vòng lõi"}
  - {id: DEC-018, type: decision,  status: active,        verified: true,  method: ran-test,    title: "/source nạp nguồn raw, transaction-LIGHT (grounded spec 10.8/15); tạo sources.md nếu thiếu"}
  - {id: DEC-019, type: decision,  status: active,        verified: true,  method: ran-test,    title: "Fix GỐC INV-16: ABSPATH_RE không bắt oan URL https:// (negative lookbehind ổ đĩa)"}
  - {id: DEC-020, type: decision,  status: active,        verified: true,  method: ran-test,    title: "/gaps read-only (liệt kê open_gaps) — khép nhóm lệnh đọc"}
  - {id: DEC-021, type: decision,  status: active,        verified: true,  method: ran-test,    title: "E2E claim/nguồn toàn-vault (1 dương + 4 âm) — nghiệm thu nhánh kiến thức confirmed"}
  - {id: DEC-022, type: decision,  status: active,        verified: true,  method: ran-test,    title: "Nghiệm thu P12 tất định: determinism + chống giả mạo FSRS (E-REVIEW-MISCALC) + portability"}
  - {id: DEC-023, type: decision,  status: active,        verified: true,  method: ran-test,    title: "Pilot Runbook (P12 bán-thủ-công) + drift-guard pilot_commands↔CLI; fix heading theo quy ước"}
  - {id: DEC-024, type: decision,  status: active,        verified: true,  method: ran-test,    title: "/ask ghi câu hỏi phụ vào ## Hỏi phụ (LIGHT); text-edit + LIGHT validate làm lưới"}
  - {id: DEC-025, type: decision,  status: active,        verified: true,  method: ran-test,    title: "/test read-only (báo cáo learned_gate) qua CR-0002; tái dùng _check_gate_and_evidence"}
  - {id: DEC-026, type: decision,  status: active,        verified: true,  method: ran-test,    title: "Chốt bề mặt lệnh 11/15; skip/system null-đúng-thiết-kế; fix defer (rủi ro); drift-guard null-set"}
  - {id: DEC-027, type: decision,  status: active,        verified: true,  method: ran-test,    title: "Fix GỐC: đường regen stage='full' sinh has_draft_knowledge (INV-26) — /done đóng được khi có draft; RED-first"}
  - {id: DEC-028, type: decision,  status: active,        verified: true,  method: ran-test,    title: "Nối dây vòng đời open_session (§5.4/§11B.2): /learn+/resume MỞ phiên, /done ĐÓNG; fix cảnh báo unclosed là code chết (RED-first)"}
  - {id: DEC-029, type: decision,  status: active,        verified: true,  method: ran-test,    title: "Enforce INV-05 phần thời gian (updated<=today) ở validator (tách khỏi model thuần); now bơm được (--at + Transaction.now) để tất định; RED-first, đóng NOTE-009"}
  - {id: DEC-030, type: decision,  status: active,        verified: true,  method: ran-test,    title: "date_policy enum-hoá Literal['local_date'] (chống cấu hình 'nói dối hành vi', theo triết lý strict); RED-first, spec §5.4"}
  - {id: DEC-031, type: decision,  status: active,        verified: true,  method: ran-test,    title: "Chống open_session dangling: /forget clear open_session khi xoá lesson đang mở (fix gốc) + validator bắt open_session.lesson_id dangling (diễn giải INV-03 'mọi tham chiếu'); RED-first x2"}
  - {id: DEC-032, type: decision,  status: active,        verified: true,  method: ran-test,    title: "Đồng nhất duyệt tất định os.walk (INV-17/18): sort dirnames + sorted(filenames) → thứ tự lỗi E-MIX-* tất định cross-machine (khép determinism §0/§1 + portable); RED-first monkeypatch"}
  - {id: DEC-033, type: decision,  status: active,        verified: true,  method: ran-test,    title: "occ_recheck ABORT khi E-CONCURRENT-EDIT (khớp spec §10.3; chống .tx kẹt 'prepared' treo); đồng nhất pre-commit fail→aborted; RED-first"}
  - {id: DEC-034, type: decision,  status: active,        verified: true,  method: ran-test,    title: "Nối dây E-IO-ENCODING ở BIÊN validator (3 entry bọc EIoEncoding→report): file non-UTF-8 báo sạch không crash (spec §10.4/§19); drift-guard tự bắt → sync validation_rules.md; RED-first"}
  - {id: DEC-035, type: decision,  status: active,        verified: true,  method: ran-test,    title: "Driver main() cũng bọc VIO.EIoEncoding→E-IO-ENCODING (3 khối dispatch): lệnh user trên file non-UTF-8 báo sạch không crash; hoàn tất robustness non-UTF-8 cả validator+driver; RED-first"}
  - {id: DEC-036, type: decision,  status: active,        verified: true,  method: ran-test,    title: "begin() dọn .tx/<id> dở khi lỗi trước-manifest (E-STALE-CONTEXT) — đóng orphan backup rò rỉ (DEC-033 known_minor a); GC 'prepared' vẫn để cho owner/P11; RED-first"}
  - {id: DEC-037, type: decision,  status: active,        verified: true,  method: ran-test,    title: "current_topic phải trỏ topic tồn tại (kiểm DIR, tôn trọng topic rỗng) — hoàn tất reference-integrity 3 con trỏ vault_state; cùng diễn giải INV-03 DEC-031; RED-first"}
  - {id: DEC-038, type: decision,  status: active,        verified: true,  method: ran-test,    title: "read_text utf-8-sig (bỏ BOM): file editor Windows có BOM không còn báo oan 'thiếu front-matter' + hết lệch driver↔validator; RED-first (pilot)"}
  - {id: DEC-039, type: decision,  status: active,        verified: true,  method: ran-test,    title: "regen đồng bộ topic_state.lessons[].status từ lesson_state (view §4/INV-25): status lesson đổi (→learned) không còn E-INDEX-MISMATCH; RED-first (pilot)"}
  - {id: DEC-040, type: decision,  status: active,        verified: true,  method: ran-test,    title: "Card.step: int → Optional[int] (py-fsrs đặt step=None ở state Review; khớp spec §29 dòng 1 + INV-08 replay bảo đúng-đắn state↔step). Fix gốc under-modeling schema; RED-first (pilot vòng 3, đạt mastered lần đầu qua CLI thật)"}
  - {id: DEC-041, type: decision,  status: active,        verified: true,  method: ran-test,    title: "Biên đọc-parse driver suy biến duyên dáng: SchemaError ở _load_raw+_lesson_model_from_raw → E-SCHEMA/E-SCHEMA-YAML sạch (không crash traceback trên file sửa-tay hỏng). Chọn bọc-biên (C) thay pre-flight (B) vì (B) chặn nhầm /forget lesson hỏng — kiểm chứng thực nghiệm. RED-first (pilot vòng 4)"}
  - {id: DEC-042, type: decision,  status: active,        verified: true,  method: ran-test,    title: "_load_vault_state validate vault_state qua M.VaultState (cùng normalize validator, không lossy-coerce) rồi trả raw → sai-kiểu (open_session=chuỗi, current_lesson=int) ra E-SCHEMA sạch thay crash AttributeError/TypeError; wire 14 site; tránh bẫy đệ quy self-reference; RED-first (pilot vòng 5)"}
  - {id: DEC-043, type: decision,  status: active,        verified: true,  method: ran-test,    title: "Lệnh write validate authored-collection khi đọc: _load_topic_state (lessons qua LessonIndexEntry, KHÔNG đụng view regen→giữ self-heal) + cmd_review validate full target lesson + cmd_forget guard review_items (giữ DEC-041 forget-rác). Xử lý BẤT ĐỐI XỨNG có chủ đích theo ngữ nghĩa. sai-kiểu → E-SCHEMA sạch; RED-first (pilot vòng 6)"}
  - {id: DEC-044, type: decision,  status: active,        verified: true,  method: ran-test,    title: "Enforce sources[].added <= today (qua CR-0004 approved) — _check_source_added_not_future ở validator, song song DEC-029. Đóng DEC-029 open_question hướng CÓ. Đi TRỌN vòng §12 (pending→approved→implement) vì mở-rộng-scope-INV-05 ngoài spec text; RED-first 4 test"}
  - {id: DEC-045, type: decision,  status: active,        verified: true,  method: ran-test,    title: "P11 lớp THỰC THI migration (migrations/executor.py: execute_step/run_plan) — atomic migrate-or-rollback qua Transaction (Q2=a). Chứng minh cơ chế bằng schema-v2-giả-lập (mock-trong-tmp, KHÔNG để lại _system); validate-at-target là SEAM inject vì validator-v2 thật chưa có (DEC-011 vẫn đúng cho transform v2 thật). RED-first 4 test; dọn sạch"}
  - {id: DEC-046, type: decision,  status: active,        verified: true,  method: ran-test,    title: "cmd_learn escape free-text (title/objective/lesson_title) vào front-matter YAML qua _yaml_scalar(json.dumps); id/date raw; body raw. Chống crash yaml.ScannerError khi objective/title có ':'/'#'/'\"' (RẤT thường). Cùng bản chất DEC-041→043 nhưng ở biên template-substitution. RED-first; phát hiện từ walkthrough Q5"}
  - {id: DEC-047, type: decision,  status: active,        verified: true,  method: ran-test,    title: "START_HERE.md ở GỐC ai-learning-system: mồi bootstrap cho AI mới (cross-AI handoff) — trình tự nạp hiến pháp→README→decisions→venv→status; pointer-heavy (không lặp→chống drift); nói thật đơn-vị-portable (spec ở thư mục cha) + điều-kiện (AI chạy-được-lệnh, venv rebuild). Drift-guard 3 test (pointer tồn tại + ép system_prompt). RED-first"}
  - {id: DEC-048, type: decision,  status: active,        verified: true,  method: ran-test,    title: "_system/selfcheck.py — first-run self-check cho bản copy (stdlib-only, chạy bằng Python hệ thống chưa cần venv): kiểm 14 file+3 dir cốt lõi + Python>=3.10 + .venv, in bước tiếp theo. Là 'cửa số 0' của START_HERE. teeth test (repo→ok, rỗng→fail). RED-first"}
  - {id: DEC-049, type: decision,  status: active,        verified: true,  method: ran-test,    title: "_system/audit.py — báo cáo CHỈ-ĐỌC 'folder đã làm gì': verdict toàn-vẹn (validator) + topics/lessons/status + tiến độ ôn + open_gaps + lịch sử (transaction_log). Robust (vault hỏng→báo cáo không crash), read-only, --at tất định. teeth test 4 (corrupt→FAIL không crash; /review→activity)"}
  - {id: DEC-050, type: decision,  status: active,        verified: true,  method: ran-test,    title: "CR-0005 approved+áp: /learn tạo topic.md (## Lộ trình + ## Knowledge Map) — roadmap cấp-topic light-touch, AI-authored, KHÔNG INV cứng (kế-hoạch-mềm, tránh coupling). topic.template.md + cmd_learn + teaching_rules; RED-first test. 378 passed"}
  - {id: DEC-051, type: decision,  status: active,        verified: true,  method: ran-test,    title: "CR-0006 approved+áp: buổi ôn ghi vào lesson.md ## Sessions (block '### Session — ôn tập'), KHÔNG tạo file/buổi (trùng §14 + xung đột INV-25). Chỉ sync review_rules; KHÔNG code/artifact mới (fix bản chất: nhu cầu đã có cơ chế). Alias /ôn hoãn"}
  - {id: DEC-052, type: decision,  status: active,        verified: true,  method: ran-test,    title: "HUONG_DAN.md (gốc ai-learning-system) — hướng dẫn NGƯỜI HỌC tiếng Việt, lệnh GIỮ English (không alias registry theo yêu cầu). Khối 'commands (máy đọc)' 19 lệnh + drift-guard test lệnh ⊆ commands.md (nhắc lệnh bịa → test đỏ). Pointer 1 chiều START_HERE (phân vai AI vs người học) + README bản-đồ. KHÔNG đưa vào selfcheck REQUIRED (user-facing, tránh coupling). RED-first 3 test; 381 passed"}
  - {id: DEC-053, type: decision,  status: active,        verified: true,  method: read-source, title: "HANDOFF_TEST.md (gốc ai-learning-system, đi theo bản copy) — prompt tự-chứa cho AI KHÁC tiếp quản: ép bootstrap đúng + khai báo điều-kiện chạy-được-lệnh; PHẦN A cơ chế (khách quan, validator+transaction_log) + PHẦN B dạy thật (Class D cần người); self-report HANDOFF_RESULT.md; RUBRIC R1..R7 CÔNG KHAI (falsifiable). Verify chữ ký CLI thật trước khi viết lệnh (không bịa). Đóng khoảng trống portability-AI-khác vs portability-mức-file. Chờ người dùng chạy → tôi đánh giá"}
  - {id: DEC-054, type: decision,  status: active,        verified: true,  method: ran-test,    title: "Tách demo docker khỏi vault ship: copy → fixture _system/validator/tests/fixtures/demo_vault (dưới validator/ → INV-18 skip, không kích E-MIX-DATA); repoint 29 file test sang fixture; learning_vault ship RỖNG (xoá topics/docker, current_topic/current_lesson=null, topics/.gitkeep giữ dir khi zip). Fix gốc coupling fixture↔dữ-liệu-người-dùng + nhầm lẫn UX 'sao có sẵn bài'. RED-first test_shipped_vault_clean (validate PASS + topics rỗng + con trỏ null). venv rebuild giữa chừng (biến mất khỏi disk). 384 passed; vault ship validate PASS; selfcheck NGUYÊN VẸN"}
  - {id: DEC-055, type: decision,  status: active,        verified: true,  method: read-source, title: "SPEC-first tính năng 'curriculum-driven-learning' (.kiro/specs/): requirements 11 nhóm EARS + design + tasks 11/9-wave, owner DUYỆT design 2026-07-05, CHƯA code. Quyết định thiết kế: reference ON-DEMAND (không clone bulk, TRD-006); exam/ NGOÀI vault (bài nộp có thể code, INV-17); bản ghi chấm trong vault ref tương đối (INV-16); schema curriculum+exam_results (drift-guard như DEC-008); topic_state.lessons[] vẫn nguồn sự thật index (INV-25), curriculum.lesson_id chỉ tham chiếu validate; 9 mã lỗi RED-first; auto-advance móc cmd_done không cổng mới; CR-0007/8/9. Design-first vì tính năng đa-chạm, giảm rework + giữ §12"}
  - {id: DEC-056, type: decision,  status: active,        verified: true,  method: read-source, title: "/ask GIỮ ghi lesson.md ## Hỏi phụ (KHÔNG tách file riêng) theo quyết định owner ('gõ /ask là được'). Spec dòng 163/1102 CỐ Ý 'không tách file riêng' → đổi phải qua CR §12. 'Rác' chủ yếu là placeholder '- (chưa có)' in sẵn. Hoãn phương án tách file (nếu làm: CR đổi §14A+cmd_ask+template+tests)"}
  - {id: DEC-057, type: decision,  status: active,        verified: true,  method: ran-test,    title: "CR-0007 approved+áp — Task 2 (curriculum-driven-learning) RED-first: model Curriculum/CurriculumPoint/ExamResults/ExamResult + schemas/curriculum.schema.md + exam_results.schema.md + drift-guard (MODEL_BY_SCHEMA+=2) + thêm curriculum.md/exam_results.md vào INV-18 _SYSTEM_DATA_NAMES. Model chỉ giữ ràng buộc CẤU TRÚC; kiểm NGỮ NGHĨA để Curriculum_Validator (Task 3) phát mã E-CURR-* phân biệt. 386 passed; validator PASS. Do trình-thực-thi-task tự chạy tasks.md (xem NOTE-033)"}
  - {id: DEC-058, type: decision,  status: active,        verified: true,  method: ran-test,    title: "Task 3.1 Curriculum_Validator: _check_curriculum phát E-CURR-DUP-ID/E-CURR-ORDER/E-CURR-EMPTY-OBJECTIVE (ngữ nghĩa model không bắt) + thêm curriculum/exam_results vào _SCHEMA_MODELS (trước THIẾU→curriculum.md bị bỏ qua, under-enforce) + wire cuối _validate_topic (curriculum tùy chọn) + sync validation_rules (coverage 2 chiều). TINH CHỈNH design: bỏ E-CURR-SCHEMA/E-CURR-BADSTATUS (trùng E-SCHEMA qua model strict/Literal → vi phạm R10.1 + ceremony) → 9 mã còn 7 thật. Scope: enforcement theo tiền lệ DEC-034, không cần CR mới. RED-first (5 fail→pass); 391 passed; validator PASS"}
  - {id: DEC-059, type: decision,  status: active,        verified: true,  method: ran-test,    title: "Task 3.2 Curriculum_Validator: +E-CURR-POINTER (current_point dangling — INV-03, cùng diễn giải DEC-031/037) +E-CURR-LESSON-LINK (point.lesson_id phải trỏ lesson thật trong all_lesson_ids — INV-25; khẳng định lại nguồn sự thật index=topic_state.lessons[], curriculum.lesson_id chỉ tham chiếu). RED-first (2 fail→pass, +2 ca hợp lệ) + sync validation_rules. 395 passed; validator PASS. 5/7 mã curriculum xong (còn REF-BROKEN cần reference/ + EXAM-REF-BROKEN)"}
  - {id: DEC-060, type: decision,  status: active,        verified: true,  method: ran-test,    title: "Task 3.3 Curriculum_Validator: +E-CURR-REF-BROKEN (source_refs trỏ file reference/ tồn tại, resolve tương đối topic_dir; abspath đã do E-PORT-ABSPATH). RED-first. Task 3 HOÀN TẤT 6/6 mã curriculum (DUP-ID/ORDER/EMPTY-OBJECTIVE/POINTER/LESSON-LINK/REF-BROKEN). 397 passed; validator PASS. Còn E-EXAM-REF-BROKEN (Task 8)"}

  - {id: DEC-061, type: decision,  status: active,        verified: true,  method: ran-test,    title: "Task 8.1 Exam ref-integrity: _check_exam_results phát E-EXAM-REF-BROKEN (ref bài nộp phải resolve vào exam/ NGOÀI vault + tồn tại; target ∈ {topic}∪{curriculum_point ids}; verdict Class D không kiểm). Wire cuối _validate_topic (exam_results tùy chọn). TÁCH gating sai tasks.md: 8.1 là enforcement schema CR-0007 (như Task 3) → KHÔNG cần CR-0008 (chỉ cmd_grade 8.2 cần). Tiền lệ DEC-034. HOÀN TẤT 7/7 mã ngữ nghĩa (6 curriculum+1 exam) → tầng validator XONG. RED-first (4 fail→pass); 401 passed; validator PASS"}

  - {id: DEC-062, type: decision,  status: active,        verified: true,  method: ran-test,    title: "CR-0008 APPROVED (owner qua proceed): move pending→approved, chốt tên lệnh (display /next-lesson ↔ backend next_lesson), changelog hoãn tới đủ 5 lệnh. Task 4.2 cmd_collect RED-first: ghi topics/<topic>/reference/<slug>.md transaction-LIGHT (slug an toàn không path/traversal; topic phải tồn tại; nội dung không rỗng; abspath→LIGHT chặn). Đăng ký ĐỒNG BỘ CLI_COMMANDS+parser+dispatch+commands.md(bảng+backends)+router (3 drift-guard xanh). Task 4.1 'reference chỉ .md' KHÔNG thêm (trùng INV-17 phần code → ceremony). 407 passed; validator PASS"}

  - {id: DEC-063, type: decision,  status: active,        verified: true,  method: ran-test,    title: "Task 5 cmd_curriculum: dựng curriculum.md từ điểm học (JSON list objective/source_refs), backend gán cp-NNN+order 1..N+current_point=cp-001+teachable=true, ghi qua _dump_state(safe_dump→free-text an toàn) transaction-FULL → _check_curriculum gate E-CURR-* (cấu trúc sai ABORT). Create-new-only (đã có→lỗi). --check GỘP vào /validate (không thêm cờ, tránh ceremony như Task 4.1). Đăng ký đồng bộ CLI/parser/dispatch/commands.md/router (drift-guard xanh). RED-first (7 fail→pass); 414 passed; validator PASS"}

  - {id: DEC-064, type: decision,  status: active,        verified: true,  method: ran-test,    title: "Task 6 cmd_next_lesson ('nhảy bài'): sinh lessons/lesson-NNN cho current_point (objective bám point), set point.lesson_id, append topic_state.lessons[] + REGEN view từ TẤT CẢ lesson (cũ đọc-đĩa + mới in-memory, VW.regen_all stage=full) → INV-25/09, trỏ vault_state.current_lesson + mở phiên; transaction-FULL (validator gate INV-25/09/08 + E-CURR-LESSON-LINK). Điều kiện teachable=true + current_point chưa có lesson. current_point KHÔNG dời (để /done auto-advance Task 7). Đăng ký /next-lesson↔next_lesson đồng bộ. RED-first (5 fail→pass); 419 passed; validator PASS"}

  - {id: DEC-071, type: decision,  status: active,        verified: true,  method: ran-test,    title: "Audit đối kháng lệnh curriculum — fix GỐC crash traceback (NOTE-018): 3 lệnh cmd_curriculum_insert/cmd_next_lesson/cmd_grade đọc RAW collection (points/results) rồi phần_tử.get('id') → AttributeError THÔ trên file sửa-tay-hỏng (YAML hợp lệ nhưng collection sai kiểu: list non-dict / không phải list). Helper CHUNG _load_model_validated(path, model) validate raw qua pydantic (M.Curriculum/M.ExamResults) TRƯỚC khi iterate → SchemaError⊂SessionError → E-SCHEMA sạch (khuôn DEC-042). insert/next_lesson validate cứng (curriculum=đối-tượng); grade valid_targets dùng guard MỀM isinstance (curriculum=tham-chiếu-phụ, target=topic vẫn chấm được); grade append exam_results validate cứng (LIGHT không post-check). Probe CLI: trước=Traceback, sau=E-SCHEMA. RED-first (5 fail→pass); 452 passed; validator PASS. Hệ sẵn sàng cross-AI test"}

  - {id: DEC-072, type: decision,  status: active,        verified: true,  method: ran-test,    title: "Fix test-defect drift-guard bootstrap (phát hiện qua cross-AI HANDOFF_RESULT báo pytest 451/1): test_bootstrap_pointers_all_exist ép MỌI pointer tồn tại, GỘP NHẦM spec_ground_truth=../PROMPT_LEARNING_SYSTEM.md (cố ý NGOÀI đơn vị, DEC-047) với pointer trong-đơn-vị → fail OAN trên MỌI bản copy portable sạch (cha không mang spec); máy dev pass do cha tình cờ có spec. Tách _required_in_unit_pointers (loại spec ngoài-đơn-vị) làm tập must-exist; spec kiểm riêng (phải '..' + KHÔNG must-exist). code(test)-lệch-design → KHÔNG cần CR (tiền lệ DEC-029). RED-first (2 fail NameError→pass, +test_portable_copy_no_parent_spec mô phỏng copy trong tmp + test_spec_ground_truth_is_external); START_HERE không đổi (đã nói thật). 454 passed (452+2); validator PASS"}

  - {id: DEC-070, type: decision,  status: active,        verified: true,  method: ran-test,    title: "Hardening HANDOFF_TEST (đóng gốc NOTE-031 'AI kia chỉ /learn rồi dừng'): tách Phần A SOLO không-cần-người làm-ĐẾN-HẾT (A1 lõi learn/validate/status/schedule/audit + A2 vòng giáo trình v2.7 collect/curriculum/--insert-at/next-lesson/grade) vs Phần B cần người-học-thật (solo→ghi bỏ, KHÔNG tính trượt); rubric +R4b (curriculum.md/exam_results sau A2); self-report tách A1/A2/B. Thêm khối 'commands (máy đọc)' + drift-guard test_handoff_commands 3 test (lệnh handoff ⊆ commands.md + phủ vòng v2.7; khớp tiêu đề exact qua md_ast). START_HERE spec_ref v2.6→v2.7. Cross-AI test THẬT vẫn chờ owner chạy (ranh giới trung thực). Thuần tài liệu + 1 guard. RED-first (heading fix); 447 passed; validator PASS"}

  - {id: DEC-069, type: decision,  status: active,        verified: true,  method: ran-test,    title: "Task 5.3/CR-0010 chèn điểm giữa chừng (R8): cmd_curriculum_insert qua CỜ /curriculum --insert-at <pos> --point <json> (KHÔNG thêm tên lệnh → drift-guard bất biến, tiền lệ --check DEC-063; dispatch route theo cờ, backend riêng). Point mới order=pos + id cp-{max_suffix+1} DUY NHẤT ổn định (id≠order, tách định-danh vs thứ-tự); order>=pos dịch +1 (giữ hoán vị 1..N+1 E-CURR-ORDER); id/status point cũ + current_point (tham chiếu ID) GIỮ NGUYÊN (R8.3); transaction-FULL re-validate E-CURR-* rollback nếu sai (R8.5); vị-trí ngoài [1..N+1]/curriculum thiếu/objective rỗng → SessionError không đổi (R8.7). R8.6 deep-FAIL là Class D (không mã máy). RED-first (9 fail→pass); 444 passed; validator PASS. TÍNH NĂNG HOÀN TẤT 100%"}

  - {id: DEC-068, type: decision,  status: active,        verified: true,  method: ran-test,    title: "Task 10 E2E nghiệm thu: test_e2e_curriculum 2 test — (1) chuỗi THẬT qua cmd_* collect→curriculum(teachable 2 điểm)→validate PASS→next-lesson(lesson-002 cho cp-001, current_point chưa dời)→mô phỏng học qua learned_gate (_make_lesson_learned: lesson_state status=learned+mastery≥_GATE + lesson.md Sessions+transcript+1 evidence/trục quote⊆transcript)→done AUTO-ADVANCE learned=True THẬT (cp-001→done, current_point→cp-002)→next-lesson-2 (lesson-003 cho cp-002), FULL PASS cuối; bổ sung đường learned→advance DEC-065 cố ý hoãn. (2) bảo toàn bất biến: vault có curriculum+exam_results (grade ex-001→cp-001) PASS toàn bộ. E2E nghiệm thu code T2-T9 → xanh-ngay đúng (RED-first áp hành vi mới). 435 passed; validator PASS. TÍNH NĂNG HOÀN TẤT T1-T10"}

  - {id: DEC-067, type: decision,  status: active,        verified: true,  method: ran-test,    title: "Task 9 khép registry/tài liệu: changelog CR-0008 (5 lệnh giáo trình áp xong, cột VERSION '—' vì mở-rộng tương thích ngược KHÔNG đổi schema-version, khác CR-0001 bump) + HUONG_DAN.md thêm mục người-học 'Học theo giáo trình' (bảng 5 lệnh + exam/ ngoài vault diễn giải INV-17 ngôn ngữ thường) + /collect /curriculum /next-lesson /grade vào khối 'commands (máy đọc)'. CR-0009 (spec bump v2.7) CHƯA ghi changelog (spec chưa soạn — §12 chỉ ghi khi áp xong, không ghi nợ trước). Drift-guard test_huong_dan/registry/router xanh. 433 passed; validator PASS. Thuần tài liệu (không code mới)"}

  - {id: DEC-066, type: decision,  status: active,        verified: true,  method: ran-test,    title: "Task 8.2 cmd_grade(topic,submission_id,file,target,verdict): append topics/<topic>/exam_results.md transaction-LIGHT. Guard NGAY trong lệnh (LIGHT không chạy _check_exam_results): submission_id pattern ^ex- + file resolve dưới exam_root=(vault.parent/exam)+is_file (thiếu/ngoài→SessionError, KHÔNG bản ghi bộ phận R9.6) + target∈{topic}∪{cp id} + không trùng (INV-04). Backend TỰ TÍNH ref tương đối os.path.relpath→as_posix (portable INV-16, chống gõ tay '../..', resolve khớp validator). import os (thay walk_up 3.12, giữ >=3.10). verdict Class D chỉ kiểm không-rỗng. Đăng ký /grade↔grade đồng bộ. RED-first (8 fail→pass); 433 passed; validator PASS. 5/5 lệnh CR-0008 XONG"}

  - {id: DEC-065, type: decision,  status: active,        verified: true,  method: ran-test,    title: "Task 7 auto-advance R7: helper THUẦN _advance_curriculum(cur_raw,lesson_id,learned,today)->bool (tất định, không I/O) + WIRE mỏng vào cmd_done. learned=regen['lesson_status'][lid]=='learned' (gate-passed literal). Map point CHƯA done → status=done + dời current_point sang point chưa-done đầu theo order; hết → GIỮ point tồn tại (hoàn tất NGẦM, KHÔNG field mới→tránh CR schema, không dangling INV-03); idempotent (point done→no-op). Ghi curriculum.md trong CÙNG transaction-FULL /done (validator gate E-CURR-* + E-GATE-FAIL trước commit). Không curriculum/chưa learned/không map → writes GIỮ NGUYÊN (backward-compat, mọi /done cũ xanh). Tách helper-thuần(5 unit) vs wire(integration no-op); learned→advance E2E để Task 10. RED-first (5 fail→pass); 425 passed; validator PASS"}

  - {id: DEC-073, type: decision,  status: active,        verified: true,  method: ran-test,    title: "Fix GỐC E-EXAM-REF-BROKEN GIẢ trong transaction-overlay (bug tích hợp THẬT do cross-AI handoff #2 Gemini phát hiện, E2E cũ DEC-068 bỏ sót vì chỉ validate STANDALONE): _build_overlay copy nội-dung-vault sang TEMP, exam/ là sibling NGOÀI vault → không có trong overlay → _check_exam_results resolve ref bài nộp + exam_root vào TEMP → false-positive → MỌI lệnh FULL-transaction SAU khi có exam_results.md bị ABORT oan. Fix: thread real_vault_root=transaction.self.root qua validate_full_semantic→core→_validate_topic→_check_exam_results; dựng lại real_topic=base/topic_dir.relative_to(vault_root) + exam_root=base.parent/exam từ vault THẬT (ref tương đối topic_dir nên phải resolve từ topic thật). Standalone (real_vault_root=None) giữ hành vi cũ. TỪ CHỐI fix hack của Gemini (detect 'tx_overlay_' + Path(__file__) hardcode layout → phá portability INV-16). RED-first: probe next_lesson trước=committed False [E-EXAM-REF-BROKEN] sau=True []; test_tx_exam_overlay.py 4 test (no-false-positive + teeth still-detects + standalone-unchanged). Không mã lỗi mới → validation_rules không cần sync. code-lệch-hành-vi-đúng → không cần CR (DEC-029/072). 458 passed (454+4); validator PASS"}

  - {id: DEC-074, type: decision,  status: active,        verified: true,  method: ran-test,    title: "Tính năng mandatory-curriculum-framework (Topic_Blueprint) HOÀN TẤT: SPEC-first (req→design→tasks) + 8 wave RED-first; schema blueprint (Blueprint+MandatoryArea) + CurriculumPoint.area_refs (Coverage_Map QĐ-1 phía curriculum editable) + Blueprint_Validator 7 mã E-BP-* + lệnh /blueprint (build/edit/approve/amend, id ổn định R1.2, đa-cờ 1-tên) + wire artifact-tùy-chọn (P9 backward-compat). CR-0011..0014 approved+changelog; spec §3.6 v2.7→v2.8; VERSION giữ =1 (additive). git init CHA + commit mỗi wave. 458→499 passed; validate --scope full PASS"}
  - {id: DEC-075, type: decision,  status: active,        verified: true,  method: ran-test,    title: "ROOT-FIX: coverage (E-BP-AREA-UNCOVERED/POINT-OUTSIDE) là CỔNG của teachable (gate: approved AND curriculum tồn tại AND teachable), KHÔNG bất biến vault độc lập. Phát hiện khi truy vết luồng approve TRƯỚC khi viết lệnh — nếu gate chỉ theo status=approved thì approved-blueprint-chưa-curriculum brick vault (lớp DEC-073) + chicken-egg approve. Suy từ R3.3 contrapositive (teachable⟹phủ đủ). E-BP-AREA-REF-BROKEN (INV-03) vẫn ungated. Teeth-probe xác nhận. code-sửa-lệch-thiết-kế-đúng → không cần CR (DEC-029/072)"}

  - {id: DEC-076, type: decision,  status: active,        verified: true,  method: ran-test,    title: "CR-0015 approved+áp (owner chọn B của TRD-008): chế độ cờ /curriculum --set-area-refs <cp-id> --area-refs <json> gắn/sửa area_refs cho Curriculum_Point ĐÃ CÓ (retrofit) → GIẢI ngõ cụt NOTE-039 (curriculum-first→áp-khung). Backend cmd_curriculum_set_area_refs REPLACE + transaction-FULL (gate E-CURR-*/E-BP-*); KHÔNG tên lệnh mới (DEC-069) / KHÔNG đổi schema (area_refs CR-0012) / KHÔNG mã lỗi mới / KHÔNG bump VERSION. Retrofit dưới blueprint draft (cổng phủ tắt DEC-075) rồi --approve. RED-first 7 unit + 1 E2E (chứng minh NOTE-039 giải); 505→513 passed; validate PASS; selfcheck NGUYÊN VẸN"}

  - {id: DEC-077, type: decision,  status: active,        verified: true,  method: ran-test,    title: "Hardening HANDOFF_TEST cho vòng khung bắt buộc v2.8: thêm Phần A3 (blueprint draft → /curriculum --set-area-refs retrofit → --approve, chứng minh ngõ cụt NOTE-039 giải) + rubric R4c + /blueprint vào khối commands máy-đọc + drift-guard test_handoff_covers_blueprint_cycle. Để AI KHÁC (cross-AI) xác minh ĐẦY ĐỦ hệ hiện tại (v2.8), không chỉ v2.7. Tiền lệ DEC-070 (thêm A2). Thuần tài liệu + 1 guard, RED-first, KHÔNG cần CR. 513→514 passed; validate PASS. Cross-AI test THẬT chờ owner chạy AI khác"}

  - {id: DEC-078, type: decision,  status: active,        verified: true,  method: ran-test,    title: "Anti-drift GỐC: thêm test_decision_journal_consistency.py canh CHÍNH nhật ký decisions/ — song ánh index.yaml ↔ .md (id trong index ⇔ heading '## <id>' trong .md, 2 chiều), id duy nhất, đặt đúng file theo tiền tố DEC/DEV/TRD/NOTE, type khớp + đủ field. Bịt lỗ hổng DUY NHẤT: nhật ký (xương sống chống-drift, START_HERE bước 3) là artifact máy-đọc duy nhất trước đây KHÔNG có guard → phát hiện drift THẬT NOTE-038 (index có, notes.md thiếu). Là test drift-guard (DEC-005/077) → không cần CR. Teeth-probe DEC-999 xác nhận. 514→519 passed; validate PASS"}

  - {id: DEC-081, type: decision,  status: active,        verified: true,  method: read-source, title: "Phase 1 R1 (spec-review gate) GIẢI qua đọc code THẬT: orchestrator author lesson.md (transcript+evidence) + lesson_state TRỰC TIẾP rồi cmd_done FULL-validate làm CỔNG → KHÔNG đổi kernel (bằng chứng: test _make_lesson_learned PASS; validate._check_gate_and_evidence gác INV-07/22/E-ASSESS-FAKEQUOTE). SỬA GỐC R-ORCH-2 của SPEC_PHASE1 (viết sai 'không bao giờ ghi vault trực tiếp' — mâu thuẫn thiết kế 'AI tự author nội dung dạy'): trust model đúng = (a) orchestrator ĐƯỢC author nội-dung-dạy; (b) chuyển-trạng-thái CHỈ qua lệnh kernel; (c) 'learned' hợp lệ CHỈ khi cmd_done COMMIT (validate PASS). Kernel là cổng, không phải thiện chí orchestrator. Cải tiến ghi-nội-dung-transactional = CR tương lai, ngoài Phase 1"}

  # (2) deviations.md — đổi so với spec ban đầu
  - {id: DEV-001, type: deviation, status: active,        verified: true,  method: read-source, title: "Bỏ giả định State.New của FSRS (F-A)"}
  - {id: DEV-002, type: deviation, status: active,        verified: true,  method: read-source, title: "So due_date thay vì due_at_utc exact (F-B)"}
  - {id: DEV-003, type: deviation, status: active,        verified: true,  method: read-source, title: "Nâng spec v2.5 → v2.6 hợp thức F-A/F-B (nay có CR-0001)"}
  - {id: DEV-004, type: deviation, status: active,        verified: true,  method: read-source, title: "schema_version dùng int, không 'semver' như spec INV-19 (spec tự mâu thuẫn)"}
  - {id: DEV-005, type: deviation, status: active,        verified: true,  method: ran-test,    title: "/resume chuyển CHỈ-ĐỌC → GHI open_session (đảo một phần DEC-016) qua CR-0003; khớp §5.4/§11B.2"}
  - {id: DEV-006, type: deviation, status: active,        verified: true,  method: ran-test,    title: "Nâng spec v2.6→v2.7 (CR-0009 approved): THÊM (tương thích ngược) §3.2 layout topic +curriculum.md/reference//exam_results.md tùy chọn + §3.4 (reference/ chỉ .md on-demand + exam/ NGOÀI vault diễn giải INV-17) + §3.5 (Curriculum/Curriculum_Point/teachable + topic_state.lessons[] vẫn nguồn index INV-25 + Curriculum_Validator 7 mã Class A) + §11A.2 (+5 lệnh) + §14 bước 4b auto-advance. Áp SAU khi code XANH (spec phản ánh cái kiểm-được, không hứa trước — CR-0009 §5). KHÔNG bump _system/VERSION=1 (curriculum/exam schema_version:1 additive, không migration; tách semver tài liệu vs schema dữ liệu DEV-004). changelog VERSION v2.6→v2.7. test_change_requests_scaffold 2 + full 433 passed; validator PASS"}

  - {id: DEV-007, type: deviation, status: active,        verified: true,  method: ran-test,    title: "Nâng spec v2.7→v2.8 (CR-0014 approved): THÊM (tương thích ngược) §3.2 layout +blueprint.md tùy chọn + §3.6 MỚI (Topic_Blueprint: Mandatory_Area ma-NNN/order/title/mandatory/source_refs + status draft|approved + Blueprint_Validator 7 mã E-BP-* + Coverage_Map qua CurriculumPoint.area_refs + phủ-là-cổng-teachable approved-gated) + §11A dòng /blueprint. Áp SAU khi code XANH (tiền lệ CR-0009/DEV-006). KHÔNG bump _system/VERSION=1 (blueprint schema_version:1 additive). CR-0014 body ghi '§3.5' là tham chiếu trước-triển-khai; spec THẬT đặt blueprint §3.6 (preamble trỏ 'xem 3.6'). changelog VERSION v2.7→v2.8. full 505 passed; validator PASS"}

  # (3) tradeoffs.md — đánh đổi
  - {id: TRD-001, type: tradeoff,  status: active,        verified: true,  method: ran-test,    title: "Transaction tự viết vs git/SQLite → CHỐT GIỮ tự viết (Q4=a owner 2026-07-04): atomicity audited vững (NOTE-021) + portable thuần-file (INV-16). open-question ĐÓNG"}
  - {id: TRD-002, type: tradeoff,  status: active,        verified: true,  method: read-source, title: "due_date vs due_at_utc (tất định)"}
  - {id: TRD-003, type: tradeoff,  status: active,        verified: true,  method: read-source, title: "Enforce INV-17/18 heuristic vs để trống"}
  - {id: TRD-004, type: tradeoff,  status: active,        verified: true,  method: read-source, title: "--require-hashes chặt vs cài lỏng"}
  - {id: TRD-005, type: tradeoff,  status: open-question, verified: true,  method: read-source, title: "Ép schema_version nhất quán cross-file vs bám đúng INV-19 (chọn: KHÔNG ép — spec không yêu cầu; chờ spec-owner)"}
  - {id: TRD-006, type: tradeoff,  status: active,        verified: true,  method: read-source, title: "Clone bulk 5 repo học vào folder vs nạp reference ON-DEMAND → CHỌN on-demand: bulk phá INV-17 (code/.git/manifest) + INV-16 (nặng) + rủi ro license + 'chạy' repo vô nghĩa với hệ text; on-demand giữ bất biến + nhẹ + hợp lệ bản quyền, tận dụng đúng thế mạnh mỗi repo. Đánh đổi: cần mạng lúc collect (offline→lỗi sạch)"}

  - {id: TRD-007, type: tradeoff,  status: active,        verified: true,  method: read-source, title: "Coverage_Map đặt ở CurriculumPoint.area_refs (phía curriculum EDITABLE) vs Blueprint.covered_by (phía KHÓA khi approved) → CHỌN area_refs: đặt dữ-liệu-hay-đổi ở phía hay-đổi, tránh xung đột R4.3 (mapping ở blueprint approved → mọi sửa curriculum phải --amend). Đánh đổi: cần CR-0012 mở rộng schema curriculum (field OPTIONAL default [] → tương thích ngược tuyệt đối). Toàn vẹn ref do E-BP-AREA-REF-BROKEN, không nhét model"}
  - {id: TRD-008, type: tradeoff,  status: active,        verified: true,  method: ran-test, title: "Áp khung lên curriculum ĐÃ teachable (NOTE-039): GHI NHẬN ràng buộc blueprint-first (A, ĐÃ CHỌN) vs XÂY lệnh retrofit area_refs (B). Chọn A vì: (1) không hỏng — validator đúng R3/R5, là thiếu-năng-lực không phải bug; (2) không vi phạm req — R5.4 backward-compat giữ, không req nào bắt buộc retrofit, R2 hình dung blueprint-first; (3) vault ship rỗng → không ai kẹt + E2E chứng minh blueprint-first đủ giá trị. B = mở rộng scope, R7.1 bắt buộc qua CR → chờ owner quyết"}

  # (4) notes.md — điều nên biết
  - {id: NOTE-001, type: note,     status: resolved,      verified: true,  method: ran-command, title: ".venv hỏng do di chuyển máy → ĐÃ dựng lại (Python 3.13)"}
  - {id: NOTE-002, type: note,     status: active,        verified: true,  method: ran-command, title: "Lock chứa sẵn hash cp313 → cài trên 3.13 OK (lo ngại 3.11-only đã bác bỏ)"}
  - {id: NOTE-003, type: note,     status: active,        verified: true,  method: ran-test,    title: "Baseline hiện tại: 519 passed (DEC-078 anti-drift GỐC: drift-guard nhật ký decisions/ index.yaml↔.md +5; DEC-077 hardening HANDOFF_TEST cho v2.8 [+A3 blueprint+retrofit, +R4c, +drift-guard /blueprint] +1; DEC-076/CR-0015 retrofit /curriculum --set-area-refs +8 [7 unit + 1 E2E chứng minh NOTE-039 giải]; trước đó 505: NOTE-038 audit đối kháng blueprint +6 regression bền, KHÔNG bug; trước đó 499 DEC-074/075 mandatory-curriculum-framework/Topic_Blueprint +41: schema blueprint+area_refs +1, Blueprint_Validator structural +9, coverage +6, lệnh /blueprint +15, curriculum area_refs +5, E2E +4, trừ gộp/đổi tên; 7 mã E-BP-*; CR-0011..0014 approved; spec v2.8). Trước đó 458 (DEC-073 fix E-EXAM-REF-BROKEN transaction-overlay +4; DEC-072 fix test-defect bootstrap +2; DEC-071 audit đối kháng robustness +5; DEC-070 HANDOFF drift-guard +3; DEC-069 Task 5.3 insert +9; DEC-068 Task 10 E2E +2; DEC-066 Task 8.2 cmd_grade +8; DEC-065 Task 7 auto-advance +6; DEC-064 Task 6 cmd_next_lesson +5; DEC-063 Task 5 cmd_curriculum +7; DEC-062 Task 4.2 cmd_collect +6; DEC-061 Task 8.1 +4; DEC-060 Task 3.3 +2; DEC-059 Task 3.2 +4; DEC-058 Task 3.1 +5; DEC-057 schema +2; DEC-054 fixture+shipped-clean +3; DEC-052 HUONG_DAN +3; nền DEC-033..051). learning_vault ship RỖNG; demo docker là fixture. CR áp: cr-0001..0008 (cr-0008 APPROVED, đang triển khai incremental — mới có collect; cr-0009 spec CÒN pending). validator full PASS. Mã lỗi +7 (6 E-CURR-* + E-EXAM-REF-BROKEN) coverage 2 chiều. Curriculum-driven-learning HOÀN TẤT 100% (T1-T11 + T5.3): schema + VALIDATOR 7/7 + 5/5 lệnh CR-0008 + auto-advance + insert-point R8 (CR-0010) + registry/HUONG_DAN + spec v2.7 (DEV-006) + E2E nghiệm thu (DEC-068). cr-0001..0010 approved+changelog. KHÔNG còn task mở. Tương lai ngoài scope: alias tiếng Việt, MCP wrap, cross-AI handoff re-test (NOTE-031)"}
  - {id: NOTE-004, type: note,     status: active,        verified: true,  method: read-source, title: "Chưa có *_prompt.md, schemas/, anti_drift_rules.md"}
  - {id: NOTE-005, type: note,     status: active,        verified: true,  method: read-source, title: "Spec gốc v2.6; dùng thư viện chuẩn, không tự viết lại"}
  - {id: NOTE-006, type: note,     status: resolved,      verified: true,  method: ran-test,    title: "Bản sao thiếu claim_rules (doc+test) → đã khôi phục bám nguồn"}
  - {id: NOTE-007, type: note,     status: resolved,      verified: true,  method: ran-command, title: "Audit trọn vẹn bản sao: chỉ claim_rules bị rớt; còn lại là chưa-dựng"}
  - {id: NOTE-008, type: note,     status: active,        verified: true,  method: ran-command, title: "Install ↔ thiết kế khớp exact (fsrs+3 lib), đã verify; không thiếu repo"}
  - {id: NOTE-009, type: note,     status: resolved,      verified: true,  method: ran-test,    title: "INV-05 updated<=today: ĐÃ enforce ở validator (DEC-029) — gap spec-vs-code đã đóng, RED-first + full suite 321"}
  - {id: NOTE-010, type: note,     status: resolved,      verified: true,  method: ran-test,    title: "Nợ tất định do DEC-029 (now=đồng-hồ-thật) — đã bơm now cố định vào 13 file test validate-standalone; suite tất định, không lệ đồng hồ"}
  - {id: NOTE-011, type: note,     status: active,        verified: true,  method: ran-test,    title: "P12 stateless-handoff: ĐÃ có auto-test tất định (3 test, teeth-verified sentinel) — phiên 2 tiếp quản chỉ từ file ở path khác; phần AI-dạy-thật vẫn thủ công"}
  - {id: NOTE-012, type: note,     status: active,        verified: true,  method: ran-test,    title: "Audit claim/source ref-integrity: ĐÚNG spec §5.5/INV-04/12/13/14 (src-* uniqueness ở model), KHÔNG có gap. Cố ý KHÔNG mở rộng (spec scope hẹp) + 3 test khoá hồi quy"}
  - {id: NOTE-013, type: note,     status: active,        verified: true,  method: ran-test,    title: "Audit coverage mã lỗi (§10.6): mọi mã PHÁT RA đều có test FAIL. Ban đầu 39, bịt E-SCHEMA-UNKNOWN/E-SCHEMA-YAML; nay 40 (thêm E-IO-ENCODING DEC-034). Cảnh báo: PowerShell truncation gây âm-tính-giả"}
  - {id: NOTE-014, type: note,     status: active,        verified: true,  method: ran-command, title: "Audit error-path driver (13 nhánh SessionError): các nhánh user-facing quan trọng ĐỀU có test; cố ý KHÔNG thêm test vặt (tránh ceremony)"}
  - {id: NOTE-015, type: note,     status: active,        verified: true,  method: ran-command, title: "Pilot tự-giả-lập-người-học qua CLI thật → tìm 2 bug tích hợp (BOM DEC-038, index-status-sync DEC-039) auto-test bỏ sót + chạy trọn lesson 'learned' đầu tiên. Ranh giới: chỉ Class A/B, không chứng minh Class D"}
  - {id: NOTE-016, type: note,     status: active,        verified: true,  method: ran-command, title: "Pilot sâu vòng 2 (review + nguồn/claim/Knowledge Map + draft INV-26 dương&âm + forget/topic rỗng): SẠCH end-to-end nội dung thật, KHÔNG bug mới (sau fix DEC-038/039). Không đổi code (tránh ceremony)"}
  - {id: NOTE-017, type: note,     status: active,        verified: true,  method: ran-command, title: "Pilot vòng 3 (review lặp → mastered): tìm bug Card.step under-modeling (DEC-040) — py-fsrs step=None ở Review crash model int. Đạt mastery=mastered lần đầu qua CLI thật; phủ trọn vòng đời step Learning→Review→Relearning. Bài học: coverage theo state-space thư viện, không theo 'chạy được 1 lần'"}
  - {id: NOTE-018, type: note,     status: active,        verified: true,  method: ran-test,    title: "Pilot vòng 4 (robustness): file on-disk sửa-tay hỏng (schema/YAML/thiếu-fm) khiến driver CRASH traceback thô — tầng đọc-parse thiếu biên lỗi. Fix DEC-041 (SchemaError → E-SCHEMA sạch). Nguyên tắc thương mại: driver KHÔNG BAO GIỜ crash traceback, luôn suy biến về mã lỗi chẩn đoán được (như validator)"}
  - {id: NOTE-019, type: note,     status: active,        verified: true,  method: ran-test,    title: "Pilot vòng 5 (robustness tầng sâu): vault_state YAML-hợp-lệ-nhưng-SAI-KIỂU (open_session=chuỗi, current_lesson=int) khiến status/schedule/resume CRASH AttributeError/TypeError — driver đọc raw dict không qua model. Fix DEC-042 (_load_vault_state validate M.VaultState). Bài học: guard tầng-parse (DEC-041) chưa đủ; field-access trên raw cũng phải qua model. Sweep xác nhận hết crash thô"}
  - {id: NOTE-020, type: note,     status: active,        verified: true,  method: ran-test,    title: "Pilot vòng 6 (robustness tầng write): topic_state.lessons/lesson_state.review_items sai-kiểu crash lệnh write (review/done/forget) TRƯỚC regen/post-validate. Fix DEC-043 xử lý BẤT ĐỐI XỨNG: validate lessons (giữ self-heal view), validate full lesson khi review, guard structural khi forget (giữ forget-rác). Nguyên tắc: validate phần KHÔNG regen/recompute; phần tái-tạo để lớp regen+post-validate+INV-08 lo. Kết thúc chuỗi robustness DEC-041→042→043"}
  - {id: NOTE-021, type: note,     status: active,        verified: true,  method: ran-test,    title: "Pilot vòng 7: AUDIT crash-safety transaction (atomicity dưới sự cố) — VỮNG, KHÔNG bug (roll-forward đúng bản chất, phủ tốt). Khoá 2 ranh giới an toàn chưa canh: (1) recovery KHÔNG roll-forward tx 'prepared' chưa-quyết-commit (all-or-nothing đầu nothing); (2) recover TỪ CHỐI E-TX-PARTIAL khi target bị sửa-ngoài (không clobber). Báo trung thực: không có bug turn này"}
  - {id: NOTE-022, type: note,     status: active,        verified: true,  method: ran-command, title: "Pilot vòng 8: AUDIT round-trip YAML driver (load→dump→load) — TRUNG THỰC, KHÔNG bug (safe_dump tự nháy timestamp→giữ str; idempotent+byte-stable cả sau review; view-hash từ canonical data không phải YAML text=double-safe). Không thêm test (ceremony). Soạn CR-0004 pending formalize DEC-029 open_q sources.added<=today theo §12 (chờ spec-owner duyệt, không tự áp)"}
  - {id: NOTE-023, type: note,     status: active,        verified: true,  method: read-source, title: "Xác minh tất định FSRS cross-platform: nghi code không quantize-trước-due (spec §29#2) → GIẢI ĐÁP không bug. py-fsrs không có hook quantize; §29#2 (v2.5) bị F-B (v2.6/DEV-002/TRD-002/CR-0001) thay CÓ CHỦ ĐÍCH bằng so due_date(ngày)+stability isclose. Code khớp đúng. Bài học: truy vết CR/deviation trước khi kết luận code-lệch-spec"}
  - {id: NOTE-024, type: note,     status: active,        verified: true,  method: ran-test,    title: "Q7 dựng _system/README.md (entry-point stateless-handoff), BỎ invariants/error_codes.md (trùng→drift) + repo_lab scaffolding + uv.lock (ceremony/thừa). Q8 rà 3 prompt: khớp spec §0/§11/§12, có drift-guard, không stale — KHÔNG sửa (tránh ceremony)"}
  - {id: NOTE-025, type: note,     status: active,        verified: true,  method: ran-command, title: "Q5 walkthrough Class D: dạy 'đệ quy' đầu-cuối (FirstPrinciples→Feynman→Socrates→teachback + 5 evidence→gate INV-07/22/22b→/done PASS) + cold handoff (path khác, status+validator PASS INV-16). Lộ bug DEC-046 (cmd_learn crash ':' trong objective). Ranh giới: tôi đóng cả teacher+learner→demo CƠ CHẾ, không phải 'hiểu thật' (Class D cần người+AI-2)"}
  - {id: NOTE-026, type: note,     status: active,        verified: true,  method: ran-command, title: "Truy quét lớp bug DEC-046 (free-text→YAML) ở /source /ask: SẠCH. cmd_source dùng safe_dump (escape), cmd_ask ghi markdown body → miễn nhiễm; /source ref abspath bị E-PORT-ABSPATH chặn đúng (INV-16). DEC-046 (cmd_learn) là instance DUY NHẤT. Đóng chuỗi điều tra root-cause"}
  - {id: NOTE-027, type: note,     status: active,        verified: true,  method: ran-command, title: "Pilot multi-topic + gaps lifecycle (nhánh cuối chưa-đi nội-dung-thật): SẠCH. Đa-topic /learn OK; cross-topic same gap-id PASS (INV-04 topic-scoped đúng); /gaps/status/schedule gộp đúng; /schedule loại 'new' verbatim §8.5 (đọc mã xác minh); dup gap→E-ID-DUP; gap add→resolve OK. Không bug. Hệ đạt cao nguyên vững"}
  - {id: NOTE-028, type: note,     status: active,        verified: true,  method: ran-command, title: "RELEASE-READINESS CHECKPOINT: 3 cam kết cốt lõi LIVE-verified trên vault thật (determinism 2-lần-giống-hệt; portability path-sâu PASS INV-16; tamper due_date→E-REVIEW-MISCALC fail-loud). Snapshot: 367 test, validator PASS, 11/15 lệnh, migration executor, journal 83 entry. Hệ sẵn-sàng-sản-phẩm GĐ1; phần còn lại trigger/human-gated"}
  - {id: NOTE-029, type: note,     status: active,        verified: true,  method: ran-command, title: "Bài học process: chạy /resume (lệnh GHI) để 'valid' START_HERE đã mutate learning_vault THẬT (set open_session) → ĐÃ khôi phục sạch. Validation phải dùng lệnh CHỈ-ĐỌC hoặc vault COPY TẠM; lệnh-GHI trên vault thật phải khôi phục ngay (vault không git → sửa tay)"}
  - {id: NOTE-030, type: note,     status: active,        verified: true,  method: read-source, title: "Soạn CR-0005 (roadmap '## Lộ trình' topic.md — MỚI thật, khuyến nghị CÓ light-touch) + CR-0006 (file-ôn-mỗi-buổi — nhu cầu ĐÃ có qua lesson.md ## Sessions §14, khuyến nghị KHÔNG tạo file riêng, tái dùng Sessions) dạng PENDING §12. Design-first, chờ owner duyệt, CHƯA code"}
  - {id: NOTE-031, type: note,     status: resolved,      verified: true,  method: ran-command, title: "[RESOLVED bởi NOTE-035] Attempt handoff TRƯỚC thất bại: AI khác CHỈ /learn tạo scaffold docker rồi DỪNG (Sessions trống, không /done, không HANDOFF_RESULT.md). Đã hardening HANDOFF_TEST (DEC-070) → attempt SAU (Claude Opus 4.6, NOTE-035) chạy trọn Phần A + để lại HANDOFF_RESULT.md → ĐẠT R1–R7. Bài học vẫn đúng: copy sạch (loại .venv) + Part B cần người học thật"}
  - {id: NOTE-032, type: note,     status: active,        verified: true,  method: ran-command, title: "Nghiên cứu nguồn học (web, có cite): roadmap.sh/developer-roadmap (thứ tự) + free-programming-books (tài liệu) + project-based-learning (đề exam) + system-design-primer (nội dung sâu) + freeCodeCamp (ý tưởng) + chính chủ (docker.com). Docker path 10 điểm (Linux→kiến trúc→container/image→Dockerfile/Hub→volumes→network→Compose→tối ưu→bảo mật→Swarm/K8s). Nền cho R1/R4 + TRD-006 (dùng on-demand)"}
  - {id: NOTE-033, type: note,     status: active,        verified: true,  method: ran-test,    title: "ĐÍNH CHÍNH DEC-055: Task 2 (schema curriculum+exam_results) ĐÃ hiện thực + CR-0007 APPROVED — phát hiện khi đọc code thật lúc định làm Task 2. Việc áp xảy ra GIỮA turn trước (384) và turn này (386) do trình-thực-thi-task tự chạy tasks.md (văn phong+tham chiếu DEC-055 trùng luồng feature; nén khỏi ngữ cảnh tôi thấy). DEC-055 'CHƯA code' chỉ còn đúng Task 3+. baseline 384→386. CẢNH BÁO đồng thời: nếu trình-thực-thi còn chạy tasks.md thì tôi KHÔNG tự làm Task 3 để tránh xung đột — chờ owner xác nhận cách điều phối"}
  - {id: NOTE-035, type: note,     status: active,        verified: true,  method: ran-command, title: "Cross-AI handoff test ĐẠT: AI KHÁC (Claude Opus 4.6 via Antigravity IDE, chạy-được-lệnh) thực thi trọn HANDOFF_TEST.md (bản hardening DEC-070: A1+A2 v2.7) trên copy Desktop → chấm rubric R1–R7: R1 selfcheck exit0, R2 451/1 (fail duy nhất = test-defect DEC-072 đã fix, KHÔNG regression), R3 validate PASS, R4 6 tx khớp self-report, R4b insert hoán vị đúng + exam ref tương đối + validate PASS, R5 N/A (SOLO), R6 ghi NOTE trong copy, R7 trung thực xuất sắc (khai thẳng test-fail + friction PowerShell + bỏ Phần B). VERDICT ĐẠT (R1–R4b + R7); hệ GIỮ TOÀN VẸN sang AI khác. Đóng NOTE-031. Gợn: copy chưa sạch (.venv/__pycache__/.pytest_cache còn) — nhẹ, AI tự rebuild venv"}
  - {id: NOTE-034, type: note,     status: resolved,      verified: true,  method: ran-command, title: "Chuyển máy THẬT lần 2 (toann → k.nguyen.manh.toan, chiều ngược NOTE-001): .venv cũ trỏ C:\\Users\\toann\\scoop python313 không tồn tại → dựng lại bằng py 3.11.9 (requires-python >=3.10 hợp lệ) + pip install deps. Re-verify TOÀN BỘ trên máy mới: 452 passed (khớp transcript) + validator full scope pass:true errors:[] + selfcheck NGUYÊN VẸN. Nâng portability từ transcript→ran-command trên MÁY THỨ HAI (3.13 cũ vs 3.11 mới đều 452 → khẳng định NOTE-002 lock đa-phiên-bản + bất biến hành vi). Chỉ .venv phải dựng lại (ngoài phần portable, đúng thiết kế INV-16); dữ liệu+công cụ+journal thuần-file mang đi hoàn hảo. Không --require-hashes lần này (cài nhanh theo dải pyproject)"}
  - {id: NOTE-036, type: note,     status: active,        verified: true,  method: read-source, title: "Cross-AI handoff #2 ĐẠT (Gemini 3.5 Flash via Antigravity, chạy-được-lệnh, py 3.11.9) trên bản copy 'ai-learning-system - Copy': R1 selfcheck NGUYÊN VẸN, R2 pytest ban đầu 454/0 (fix DEC-072 hiệu quả; sau writes thật shipped_vault_clean fail 2 test ĐÚNG-THIẾT-KẾ), R3 validate PASS, R4/R4b tx khớp self-report + insert hoán vị + exam ref tương đối, R5 N/A SOLO, R7 trung thực xuất sắc (khai thẳng đã tự sửa validate.py). ĐIỂM CỘNG LỚN: phát hiện bug tích hợp THẬT E-EXAM-REF-BROKEN transaction-overlay (pilot/E2E của ta bỏ sót) → fix GỐC ở DEC-073. Fix của Gemini (trong copy: 'tx_overlay_'+__file__) KHÔNG dùng (phá portability). Ranh giới: tôi KHÔNG quan sát Gemini chạy — chấm qua đọc HANDOFF_RESULT.md (read-source) + tái kiểm ĐỘC LẬP bug qua probe (ran-command). Journal copy tách biệt, không merge ngược"}
  - {id: NOTE-037, type: note,     status: resolved,      verified: true,  method: ran-command, title: "Chuyển máy THẬT lần 3 (k.nguyen.manh.toan → toann, về lại chiều NOTE-001): .venv cũ trỏ Python máy k.nguyen.manh.toan + `py` launcher KHÔNG cài trên máy toann → dựng lại venv bằng scoop python 3.13.12 (C:\\Users\\toann\\scoop\\apps\\python313) + pip install deps (3.13 hợp lệ dải >=3.10, NOTE-002). Re-verify TRƯỚC khi fix: 454 passed + validate --scope full --at pass:true + selfcheck NGUYÊN VẸN. Chỉ .venv dựng lại (đúng INV-16); dữ liệu+journal thuần-file nguyên vẹn. Bài học: máy toann dùng scoop (KHÔNG có 'py' như máy kia) → dựng venv bằng đường dẫn scoop python313"}
  - {id: NOTE-038, type: note,     status: active,        verified: true,  method: ran-test,    title: "Audit đối kháng tính năng blueprint (chủ động, theo phương pháp đã lộ DEC-073) — KHÔNG tìm thấy bug (báo trung thực). Probe các khoảng happy-path E2E bỏ sót, thành 6 regression BỀN (test_blueprint_audit.py): A1 /done auto-advance DƯỚI blueprint approved (DEC-065×blueprint, _check_blueprint chạy trong transaction-overlay FULL, area_refs bảo toàn, coverage giữ) → commit PASS; A2 overlay tường minh không false-positive (blueprint/curriculum TRONG vault, khác exam sibling DEC-073); A3 robustness blueprint.md sửa-tay-hỏng (areas=chuỗi/status ngoài enum) → E-SCHEMA sạch không crash (lớp DEC-071, qua _load_blueprint_validated); A4 INV-16 source_refs abspath → E-PORT-ABSPATH; A5 tất định thứ tự mã E-BP-*; A6 amend approved XÓA area đang-được-tham-chiếu → E-BP-AREA-REF-BROKEN rollback (toàn vẹn giữ). 499→505 passed; validate --scope full PASS. Kết luận: tầng blueprint vững dưới tích hợp+overlay+robustness — KHÔNG như DEC-073 (lần đó exam/ sibling NGOÀI vault mới sinh false-positive; blueprint nằm TRONG vault nên overlay-an-toàn by-construction, nay có test khoá)"}
  - {id: NOTE-039, type: note,     status: resolved,      verified: true,  method: read-source, title: "⚠️ Giới hạn workflow: curriculum teachable dựng TRƯỚC (không area_refs) → KHÔNG approve được blueprint về sau (E-BP-POINT-OUTSIDE/AREA-UNCOVERED) + KHÔNG có lệnh gắn area_refs cho point đã có (cmd_curriculum từ chối nếu tồn tại; insert chỉ thêm point mới). Blueprint-first là luồng DUY NHẤT đi được. KHÔNG phải bug (validator đúng R3/R5) + KHÔNG vi phạm req (R5.4 giữ). Xử lý theo TRD-008 = ghi nhận ràng buộc. Luồng đúng: /blueprint→[--approve]→/curriculum có area_refs"}
  - {id: NOTE-040, type: note,     status: active,        verified: true,  method: ran-test,    title: "Chuyển máy THẬT lần 4 (về toann): .venv trỏ máy cũ k.nguyen.manh.toan + không có 'py' → rebuild scoop python3.13.12 + pip --require-hashes + pytest. Re-verify: selfcheck NGUYÊN VẸN, 505 passed, validate --scope full pass:true. Thư mục CHA ĐÃ là git repo (HEAD 788b440, 9 commit từ blueprint DEC-074) — khác NOTE-001/034/037. CẢNH BÁO: có bản sao lồng system-prompt2/ làm ô nhiễm grep → luôn excludePattern 'system-prompt2/**'; KHÔNG tự xóa (backup owner). 3.13.12 khẳng định lại NOTE-002 lock đa-phiên-bản"}
  # === (phiên 2026-07-09) tài liệu chiến lược (repo tương tự + luận đề sản phẩm) + phân tích chống-drift ===
  - {id: DEC-079, type: decision,  status: active,        verified: true,  method: read-source, title: "Phiên 2026-07-09: lưu tài liệu chiến lược NGOÀI cây governed — PRODUCT_THESIS.md (gốc dự án) + repo_evaluations/similar_systems_landscape.md (role prior_art_comparison); KHÔNG guard prose bằng test (tránh ceremony R10.1)"}
  - {id: DEV-008, type: deviation, status: active,        verified: true,  method: read-source, title: "Phiên 2026-07-09: đổi 'TẠO thư mục 4-việc mới' → CẬP NHẬT _system/decisions/ đã tồn tại (theo chỉ dẫn owner 'nếu có rồi thì cập nhật'); tạo mới sẽ phá guard duy-nhất-id/bijection DEC-078"}
  - {id: TRD-009, type: tradeoff,  status: active,        verified: true,  method: read-source, title: "Phiên 2026-07-09: nơi lưu tài liệu chiến lược — gốc dự án + repo_evaluations (CHỌN, tối thiểu coupling) vs trong _system/decisions vs thư mục governed mới (ceremony)"}
  - {id: TRD-010, type: tradeoff,  status: active,        verified: true,  method: read-source, title: "Phiên 2026-07-09: bước chống-drift kế tiếp — HOÃN mở rộng guard kiểm evidence-liveness (đúng gốc nhưng free-text→false-positive; design-first) vs giữ structure-only (DEC-078 đã gốc cho bijection); ghi open-question NOTE-042"}
  - {id: NOTE-041, type: note,     status: active,        verified: true,  method: ran-test,    title: "Phiên 2026-07-09 (máy k.nguyen.manh.toan): ban đầu KHÔNG có _system/.venv; ĐÃ dựng venv + chạy THẬT pytest → 520 passed + validate --scope full PASS trên workspace này (gap 'chưa chạy pytest' đã đóng). arXiv 2605.11032 không fetch được toàn văn. repo_lab/repo_evaluations KHÔNG có test guard (grep)"}
  - {id: NOTE-042, type: note,     status: resolved,      verified: true,  method: read-source, title: "[open-question] Chống-drift còn hở: guard nhật ký kiểm CẤU TRÚC (bijection/id/file/type/fields) nhưng KHÔNG kiểm EVIDENCE-LIVENESS — entry verified:true+ran-test có thể trỏ test đã đổi tên/xoá → nhật ký 'nói dối' âm thầm; đề xuất deepening có kiểm soát false-positive, chờ owner (TRD-010)"}
  - {id: DEC-080, type: decision,  status: active,        verified: true,  method: ran-command, title: "Chống-drift GỐC sound: thêm test_supersede_pointers_resolve (supersede pointers phải trỏ id thật) vào guard nhật ký — bổ sung bijection DEC-078; TỪ CHỐI liveness token evidence sau khi ĐO ~19% false-positive (28/149) trên entry hợp lệ (path một phần/ví dụ/ellipsis). Additive tiền lệ DEC-005/078. py-3 harness: 6/6 green + teeth (inject DEC-99999→đỏ)"}
  - {id: NOTE-043, type: note,     status: active,        verified: true,  method: ran-command, title: "QĐ-1 (người dùng mục tiêu) ĐÃ CHỐT = A (người học cuối) — owner quyết qua user_input 2026-07-09. Kích hoạt: QĐ-2 (giải 'phụ thuộc AI chạy CLI' → MCP/service) là nút thắt kế tiếp; cần UI; QĐ-3 (đo Class D) là đầu tư cốt lõi; kernel GIỮ không rebuild. Ghi ở notes vì là quyết định KINH DOANH của owner, không phải AI tự-ra"}
  - {id: NOTE-044, type: note,     status: active,        verified: true,  method: ran-command, title: "QĐ-2 (cơ chế phân phối/đảm bảo) ĐÃ CHỐT = A2 (app local-first nhúng kernel làm service tin cậy + tự điều phối LLM) — owner qua user_input 2026-07-09. Sản phẩm chạy validator/transaction (không phụ thuộc AI người dùng); local-first giữ privacy/markdown; MCP kênh phụ; Cloud SaaS loại. Kernel GIỮ, chỉ bọc UI+orchestrator+LLM-adapter. Chi tiết A2 design SAU khi chốt QĐ-3"}
  - {id: NOTE-045, type: note,     status: active,        verified: true,  method: ran-command, title: "QĐ-3 (đo chất lượng dạy/Class D) ĐÃ CHỐT = C3 (cổng-quá-trình + retention) — owner qua user_input 2026-07-09. 2 trục proxy máy-kiểm: (1) learned chỉ khi dẫn chứng verbatim đủ 5 trục + chống bịa; (2) retention FSRS. Ranh giới: proxy KHÔNG phải chứng minh hiểu thật. → 3 QĐ luận đề đủ; next = DESIGN_A2.md"}
  - {id: NOTE-047, type: note,     status: active,        verified: true,  method: read-source, title: "Owner chỉ-thị 'duyệt theo khuyến nghị' → DIỄN GIẢI là duyệt Phase 0 DESIGN_A2 (SD-1 local-web / SD-2 import in-process=đúng API 520 test / SD-3 BYO-key+local / SD-4 offline-kernel; M1 cổng-quá-trình đã enforce + M2 retention read-only) và cho đi Phase 1. Phase 1 làm TRƯỚC = orchestrator tối thiểu (không M2) vì chứng minh trực tiếp lời hứa QĐ-2 'sản phẩm ép kernel'. TRUNG THỰC: là diễn giải chỉ-thị chung, VẪN chỉnh được. Bước kế = SPEC_PHASE1_ORCHESTRATOR.md design-first, CHƯA code"}
  - {id: NOTE-046, type: note,     status: active,        verified: true,  method: ran-test,    title: "Chuyển máy lần 5 (ENDGAME) — tiếp quản từ transcript end.md (phiên máy k.nguyen.manh.toan đứt 'connection interrupted' lúc viết DESIGN_A2.md). Kiểm THẬT: mọi việc phiên trước CÓ MẶT (git develop a992744 sạch sync origin; venv trỏ toann/scoop py3.13.12 TỒN TẠI trên máy này nên chạy được, không dựng lại); re-verify 520 passed + validate --scope full pass:true + selfcheck NGUYÊN VẸN + journal 143→144 guard xanh. HOÀN TẤT DESIGN_A2.md §6–§10 (SD-1 local-web / SD-2 import in-process = đúng API 520 test / SD-3 BYO-key+local / SD-4 offline; định nghĩa M1 cổng-quá-trình [đã enforce] + M2 retention [read-only từ log FSRS]; rủi ro + 5 pha validate). Design-first, CHƯA code; SD/M là khuyến nghị chờ owner duyệt (§10). DESIGN_A2 ngoài governed tree (DEC-079) → suite vẫn 520"}

open_questions:
  - "TRD-001 ĐÃ ĐÓNG (2026-07-04, Q4=a): GIỮ transaction tự viết (atomicity audited NOTE-021 + portable INV-16). Không còn treo."
  - "P10 prompts (system_prompt/router_prompt/system_change_prompt): prose vận hành — Q8 rà theo spec §11/§20 (turn này/kế)."
  - "DEC-029 open_q ĐÃ ĐÓNG (2026-07-04): sources[].added <= today → CÓ, qua CR-0004 approved + DEC-044."
  - "TRD-005 ĐÃ CHỐT HOÃN (2026-07-04, Q3): cross-file schema_version consistency — hoãn tới khi P11 migration rõ (trạng thái pha-trộn version khi migrate có thể hợp lệ tạm). Xét lại khi có bước migration thật."
  - "P11 lớp thực thi: executor.py ĐÃ dựng (DEC-045, Q2). CÒN: bước v1_to_v2.py THẬT + validator-version-đích + cache.py/diff.py (performance) — chờ schema v2 thật / vault lớn (DEC-011)."
  - "CR-0005 ĐÃ APPROVED+ÁP (2026-07-04, DEC-050): roadmap '## Lộ trình' trong topic.md (light-touch)."
  - "CR-0006 ĐÃ APPROVED+ÁP (2026-07-04, DEC-051): buổi ôn ghi vào lesson.md ## Sessions (không file riêng). CÒN tùy chọn: alias /ôn=/review (hoãn — đụng registry)."
  - "TRD-008 ĐÃ ĐÓNG (2026-07-08, owner chọn B 'Có'): đã xây lệnh retrofit /curriculum --set-area-refs (CR-0015/DEC-076) → luồng curriculum-first→áp-khung đi được, NOTE-039 resolved. 513 passed."
